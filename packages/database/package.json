{
  "name": "@repo/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc --emitDeclarationOnly && tsup src/index.ts --format cjs,esm",
    "predev": "prisma generate && tsc --emitDeclarationOnly",
    "dev": "tsup src/index.ts --format cjs,esm --watch"
  },
  "dependencies": {
    "@prisma/client": "6.14.0",
    "prisma": "^6.14.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:^",
    "tsup": "^8.5.0"
  }
}